name: retorfit_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  retrofit: ^4.4.0
  json_annotation: ^4.9.0
  provider: ^6.1.5+1

dev_dependencies:
  build_runner: ^2.4.8
  json_serializable: ^6.9.0
  retrofit_generator: ^10.0.5



flutter:
  uses-material-design: true
